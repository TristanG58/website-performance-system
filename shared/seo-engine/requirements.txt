# shared/seo-engine — Python-Abhängigkeiten (vendored Drift-Motor)
# Nur der Kern für baseline/compare ohne CWV. Pins CVE-bewusst, aus dem
# Original requirements.txt von claude-seo übernommen.
# Python 3.10+ erforderlich (macOS-System-Python 3.9 reicht NICHT).

beautifulsoup4>=4.12.0,<5.0.0
requests>=2.32.4,<3.0.0
lxml>=6.1.1,<7.0.0
validators>=0.22.0,<1.0.0
